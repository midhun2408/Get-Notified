import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.getnotified.app',
  appName: 'Get-Notified',
  webDir: 'dist/get-notified'
};

export default config;
